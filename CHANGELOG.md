# 更新日志

本文件记录 Selene 相对上一正式版本的最终重要变化，不记录未发布阶段的中间过程。

## [1.8.7] - 未发布

### 修复

- 修复 Rolling 发布说明未包含待发布更新内容的问题。
- 修复 Rolling 预发布未按发布时间置顶的问题。
- 提升 Android 发布构建在依赖服务临时限流时的稳定性。

## [1.8.6] - 2026-08-05

### 新增

- 新增带待发布版本标识的多平台 Rolling 测试包，正式发布成功后自动清理。

### 变更

- 网络点播预加载范围延长至约 3 分钟，减少网络波动造成的卡顿。

### 修复

- 修复 Rolling 构建成功后未发布测试包的问题。
- 修复播放已下载视频时误用网络缓存策略，以及投屏误用本地文件地址的问题。

## [1.8.5] - 2026-08-03

### 新增

- 视频播放新增会话级临时缓存，并针对网络点播、直播和本地文件使用不同缓存策略。
- 播放进度条新增已缓存范围显示，便于判断可直接跳转的内容。

## [1.8.4] - 2026-07-31

### 新增

- Android 支持在应用内下载并校验、安装更新包。

### 变更

- GitHub Release 与客户端更新说明统一读取 `CHANGELOG.md`，并完善社区贡献的更新记录流程。

## [1.8.3] - 2026-07-29

### 新增

- 建立独立的 GitHub Releases 自动发布流程，统一构建并发布已签名 Android APK、Windows x64 便携版、macOS Universal DMG（兼容 Apple Silicon 与 Intel）和 SHA-256 校验文件。

### 变更

- 更新检测和项目链接切换至当前独立维护仓库，发布构建统一锁定依赖与 GitHub Actions 版本。

### 修复

- 完善 Android 签名证书、SDK/NDK、Windows FFmpegKit 和发布资产校验，阻止错误或不完整版本发布。
- 修复 `--macos-only` 错误要求 Android ARMv7 产物的问题。

## [1.8.2] - 2026-07-28

### 新增

- 下载管理新增完成文件导出，可通过系统保存窗口将 MKV 副本保存到用户选择的位置，并保留应用内离线文件。

## [1.8.1] - 2026-07-28

### 修复

- 修复登录请求期间或失败后，服务器地址、用户名和“记住登录”被旧连接资料覆盖的问题。
- 自动登录因断网或服务器无法连接而失败时，登录页恢复已保存密码，保留完整表单以便直接重试。

## [1.8.0] - 2026-07-26

### 新增

- 登录页新增“记住登录”，通过平台安全存储保存用户明确允许持久化的密码，并接入系统自动填充。
- 新增统一的 `Result`、`Command`、不可变 UI State、强类型 GoRouter 路由和架构依赖测试。

### 变更

- 应用升级为分层 MVVM，统一 View、ViewModel、Repository、Service 的依赖方向、状态管理和资源所有权。
- 首页、分类、搜索、直播、播放器、下载、设置、主题和更新迁移至统一的功能模块与数据边界，移除旧目录和重复实现。
- 使用 Dio、内存 CookieJar 和统一会话状态管理普通请求、SSE、启动恢复与退出；并发 401 只触发一次重新登录，可重放请求最多重试一次。
- 统一账号、服务器、运行模式和订阅变化时的缓存失效，阻止旧身份或旧订阅的在途响应重新写回。
- 目录分页使用响应式预取距离，适配不同视口和网格布局。
- Flutter、Dart 工具链及相关依赖升级至当前项目基线。

### 安全

- 会话失效或退出时清除密码和认证状态，同时保留服务器地址与用户名作为登录预填信息。
- Android 关闭 Auto Backup；Release 构建不再信任用户安装的 CA。

## [1.7.0] - 2026-07-24

### 新增

- 支持 HLS 和普通 HTTP/HTTPS 点播视频下载，并以无损流复制方式封装为 MKV。
- 新增下载管理，支持单集、多集和全部剧集下载，以及取消、重试和删除任务。
- 支持多个独立任务同时下载，可在下载管理中配置 1～5 个并发任务，默认为 3 个。
- 支持显示下载进度、已下载大小和真实有效吞吐速度。
- 支持应用重启后的任务恢复、下载文件校验和本地离线播放。
- 新增开源许可入口，展示 FFmpegKit 等第三方依赖许可证。

### 变更

- Android 最低版本调整为 API 24，以满足 FFmpegKit 预编译包要求。
- iOS 最低部署版本调整为 14.0。
- 完善项目说明、配置说明、目录结构和开发构建文档。
